import { Component } from '@angular/core';

/**
 * @deprecated This component is no longer routed.
 * Replaced by the participation request flow in the Overview tab.
 */
@Component({
  selector: 'app-epreuves',
  standalone: true,
  template: `<p>Cette page n'est plus disponible.</p>`,
})
export class EpreuvesComponent {}
